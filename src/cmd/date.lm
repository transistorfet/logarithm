#
# Command Name:	date.lm
# Version:	0.1
# Package:	Unix Utilities
#

$module_info = {
	'help' => [
		"Usage: date",
		"Description: Displays the current date"
	]
};

sub do_date {
	local($irc, $msg, $privs) = @_;

	irc_private_msg($irc, $msg->{'respond'}, `date`);
	return(0);
}
